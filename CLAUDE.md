# WinRM poller — LibreNMS module on this branch

@docs/WINRM_DESIGN.md

The design/status doc above is the current source of truth for this module (`feature/winrm-poller-claude`). Check its "Implementation status" section before assuming something is or isn't built yet — Phase A is implemented and running as of 2026-08-08; only Phase B (real Kerberos/JEA execution) is still blocked, on infra not yet provisioned, not on a design decision.
